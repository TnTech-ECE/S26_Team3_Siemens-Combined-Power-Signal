# What is this folder

Since the group is working across multiple different devices, this folder is used to aggregate the sources. Since it is not a part of the initial github repo, the team will add folders as the team sees fit. This is more so used to help the team with the design rather than documentation.